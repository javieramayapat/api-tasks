version: '3'

services:

    app:
        container_name: app
        build:
            context: .
            dockerfile: ./compose/local/Dockerfile
        volumes:
            - .:/app
        ports:
            - "8000:8000"

volumes:
    pgdata: